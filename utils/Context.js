import { createContext } from "react";

//Create Tab context to pass params to stack navigators
export const TabContext = createContext();

//Create a context to pass params to subject analysis stack
export const SubjectAnalysisContext = createContext();

//Create context for get results stack navigator
export const GetResultsContext = createContext();
